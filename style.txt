/* Сброс отступов */
html, body {
    margin: 0;
    padding: 0;
    font-family: sans-serif;
    /* Плавная прокрутка при клике по якорю */
    scroll-behavior: smooth;
  }
  
  /* Хедер по вашему желанию можно тоже зафиксировать, 
     но пока оставим статичным. 
     Фон - через bs-dark-rgb, чтобы соблюсти условие. */
  .bg-custom-dark {
    background-color: rgba(var(--bs-dark-rgb), var(--bs-bg-opacity)) !important;
  }
  
  /* САЙДБАР (фиксированный) */
  #sidebar {
    position: fixed;
    top: 0;
    left: 0;
    width: 220px;          /* ширина боковой панели */
    height: 100vh;         /* растягиваем на всю высоту окна */
    background-color: rgba(33, 37, 41, 0.95); /* темно-серый, немного прозрачный */
    padding-top: 60px;     /* отступ сверху (чтобы не налезал на header), если header не зафиксирован, можно уменьшить */
    box-shadow: 2px 0 5px rgba(0,0,0,0.3);
    z-index: 999;          /* чтобы было выше остального контента */
  }
  
  /* Контент должен сместиться вправо, 
     чтобы не перекрывать сайдбар */
  #mainContent {
    margin-left: 220px; /* та же ширина, что у сайдбара */
    padding: 1rem;
  }
  
  /* Стили ссылок в сайдбаре */
  #sidebarNav .nav-link {
    padding: 0.75rem 1rem;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    transition: background-color 0.3s ease;
    color: #fff !important;
  }
  
  /* Наведение */
  #sidebarNav .nav-link:hover {
    background-color: rgba(255, 255, 255, 0.1);
    color: #fff;
  }
  
  /* Активная ссылка */
  #sidebarNav .nav-link.active {
    background-color: rgba(255, 255, 255, 0.2);
    color: #fff;
  }
  
  /* Пример базовых стилей для секций */
  section {
    padding: 1rem 0;
    border-bottom: 1px solid #eaeaea;
  }
  
  section h2 {
    font-size: 1.5rem;
    font-weight: 600;
  }
  
  /* Дополнительная адаптация под мобильные экраны */
  @media (max-width: 768px) {
    #sidebar {
      width: 100%;
      height: auto;
      position: static; /* На мобильных можем отключить фиксированное положение */
      box-shadow: none;
    }
    #mainContent {
      margin-left: 0; 
    }
  }
  